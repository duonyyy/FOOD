# menu

Owner đích: Food, Category, Topping, availability và menu pricing hiện hành. Compatibility implementation: `src/modules/food`, `src/modules/category`.

T2.3 exports `MenuReaderPort`; T4.3–T4.6 sẽ bind adapter và chuyển vertical slices. Orders nhận snapshot qua public contract, không nhận TypeORM entity.
