# restaurants

Owner đích: Restaurant, owner reference, onboarding, approval và restaurant status. Compatibility implementation: `src/modules/restaurant`.

T2.3 exports `RestaurantReaderPort`; T4.1–T4.2 sẽ bind adapter và chuyển vertical slices. Menu và Orders chỉ dùng contract, không dùng Restaurant repository.
