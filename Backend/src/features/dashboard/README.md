# dashboard

Owner đích: reporting queries, projections and read models. Compatibility implementation: `src/modules/dashboard`.

T8.3 replaces live cross-feature repositories with read models. This feature does not own write entities from Identity, Orders or Delivery.
